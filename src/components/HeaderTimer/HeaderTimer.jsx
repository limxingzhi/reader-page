import React from "react";
import { formatRelative } from 'date-fns';
import { readLS } from '../../utils/utils';

import './HeaderTimer.css';

export default () => {
  return (<span className="rdr-headertimer-title">RSS Reader</span>);
}
